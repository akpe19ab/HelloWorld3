import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    inputField: {
        borderWidth: 1,
        margin: 10,
        padding: 10,
    },
    error: {
        color: 'red',
    },
});
export default styles

