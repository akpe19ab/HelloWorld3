import React from 'react'
import {View, Text, Button } from "react-native"

export default () => (
    <View>
        <Text> Hej2</Text>
        <Button title={"change page"} onPress={() => 5}/>
    </View>
)